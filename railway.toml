[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c 'uvicorn backend.api.server:app --host 0.0.0.0 --port ${PORT:-8001} & python -m backend.jobs.worker & wait'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

