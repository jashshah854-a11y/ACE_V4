[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m uvicorn server:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Railway Volume for persistent caching
[[deploy.volumes]]
name = "ace-cache"
mountPath = "/app/data/cache"
