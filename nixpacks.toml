[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = [
    "pip install -r backend/requirements.txt"
]

[phases.build]
cmds = []

[start]
cmd = "uvicorn backend.api.server:app --host 0.0.0.0 --port $PORT"

# No [start] section - using Procfile for start command
# Railway Volume: Configure in Railway dashboard
# - Name: ace-cache
# - Mount Path: /app/data/cache
